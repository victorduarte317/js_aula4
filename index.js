/**
 * Criar uma variável com "=" indicando que um valor
 * está sendo atribuído à tal variável.
 */

let n = 7;

for (let i=0; i <= 10; i++) {

    console.log(
        `${i} X ${n} = ${i*n}` // Template String
    ) 
    
    
    // console.log(i + 'x' + n + '=' + (i*n)); Concatenação de variáveis e valores.(Modo antigo)

}